#include <stdio.h>
#include <string.h>
 
int main() {
  	char s1[100], s2[50];
  	int i = 0, j = 0;
  	printf("25331A05G3\n");
  	printf("Enter first string: ");
  	gets(s1);
  	printf("Enter second string: ");
  	gets(s2);
  	while (s1[i] != '\0') i++;
  	while (s2[j] != '\0')
        	s1[i++] = s2[j++];
s1[i] = '\0';
  	printf("Concatenated: %s\n", s1);
  	return 0;
}
